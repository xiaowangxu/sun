const num=100;
Var a1,b2;
Begin
	Read(A1);
	b2:=a1+num;
	write(A1,B2);
End.

const a=10,b=20;
var sum;
procedure add;
  begin
    sum := a+b;
    write(sum)
  end;
call add

# sPARks PL/0 Hello World !!
# 在这里键入PL/0 程序
var a, b, c;
begin
read(a, b, c);
if a+b > c then
  if a+c > b then
    if c+b > a then
      write(1)
    else
      write(0)
  else
    write(0)
else
  write(0)
end.